{
  "scripts": {
    "clean": "rimraf ./ical.widget ./ical.widget.zip",
    "build": "yarn clean && tsc && ncp ./icalBuddy ./ical.widget/icalBuddy && cross-zip ./ical.widget"
  },
  "devDependencies": {
    "@types/react": "^16.8.14",
    "cross-zip-cli": "^1.0.0",
    "ncp": "^2.0.0",
    "rimraf": "^2.6.3"
  },
  "dependencies": {}
}
